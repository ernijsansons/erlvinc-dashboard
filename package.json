{
  "name": "foundation-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "pnpm run build && wrangler deploy"
  },
  "dependencies": {
    "@foundation/shared": "workspace:*",
    "@sveltejs/kit": "latest",
    "@sveltejs/adapter-cloudflare": "latest",
    "drizzle-orm": "latest",
    "svelte": "^5.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "^5.7.0",
    "vite": "latest",
    "@sveltejs/vite-plugin-svelte": "latest",
    "wrangler": "^4.66.0"
  }
}
