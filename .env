# Dummy environment variables for Cloudflare Pages static build
# These are only needed for the build process, not runtime

# Supabase (dummy values)
NEXT_PUBLIC_SUPABASE_URL=https://dummy.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=dummy_anon_key_for_build_only
SUPABASE_SERVICE_ROLE_KEY=dummy_service_role_key_for_build_only

# OpenAI (dummy value)
OPENAI_API_KEY=sk-dummy_key_for_build_only

# MCP (dummy value)
NEXT_PUBLIC_MCP_SERVER_URL=http://localhost:3000
