/**
 * Local shared types for standalone deployment.
 * These types are copied from $lib/shared to enable
 * deployment to erlvinc-dashboard without workspace dependencies.
 */

export * from './project-documentation';
export * from './planning-phases';
